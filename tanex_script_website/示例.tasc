include "All";
main {
    // bubble sort //;
    [arr] = {none}; [i] = '0';
    while [arr][[i]] & [i] != '0', {[arr] += int input; i += '1';};
    for [i], range '0', length [arr], {
        for [j], range '0', [arr] -[i] - '1', {
            if [arr][[j]] > [arr][[j + '1']], {swap [arr][[j]], [arr][[j] + '1'];};
        };
    };
};
end;